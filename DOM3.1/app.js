console.log("Hello world");

//let wrapper = document.querySelector(".wrapper");
//let boxEls = document.querySelectorAll(".box");

//let box5 = document.createElement("div");
/*
box5.style.cssText = `
    width: 100px;
    height: 100px;
    background-color: green; 
    font-size: 30px;
    font-family: Arial, Helvetica, sans-serif;
    color: #FFF;
`*/
//box5.classList.add('box');

//box5.innerText = 5;

//wrapper.append(box5);
//wrapper.prepend(box5);
//boxEls[1].append(box5);

//boxEls[1].before(box5);
//boxEls[1].after(box5);
//boxEls[3].replaceWith(box5);
//boxEls[1].remove(box5); // not remove the box5 , has a bag

// Older versions
//wrapper.appendChild(box5); // instead of append
//wrapper.insertBefore(box5, boxEls[1]); // instead of before & after
//boxEls[3].replaceChildren(box5); // instead of replaceWith;

//wrapper.insertAdjacentElement("afterbegin" , box5) // prepend(box5)
//wrapper.insertAdjacentElement("afterend" , box5) // after(box5)
//wrapper.insertAdjacentElement("beforebegin", box5); // before(box5)
//wrapper.insertAdjacentElement("beforeend", box5); // append(box5)

//wrapper.insertAdjacentText('beforebegin', 'Hello world');
//wrapper.insertAdjacentHTML('afterend', '<h1 style="color: #FFF;">Hello</h1>')



